language: python
python: '2.7'
cache:
  directories:
    - $HOME/.pip-cache/
install:
  - pip install ansible --download-cache $HOME/.pip-cache
script:
  - rake test_Debian
notifications:
  email:
    recipients:
      - lexrus@gmail.com
    on_success: change
    on_failure: always